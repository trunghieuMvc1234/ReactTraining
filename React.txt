Automatic render data no need to reload page
Could be combine any stack
React is component base
component can reuse

jsx -> html + javascript
reuse component -> not dependent, easy to test with each component, when test all component ok then applicarion is ok
abstraction -> hidden our implementation, make code simple, easy to read

state -> that can change data in component